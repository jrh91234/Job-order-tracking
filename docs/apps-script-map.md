# Apps Script Deployment Map

ระบบนี้ใช้ Google Apps Script **2 โปรเจกต์แยกกัน** เอกสารนี้คือแหล่งอ้างอิงหลัก
ว่าอะไรอยู่ที่ไหน เปิดหาอย่างไร และห้ามทำอะไร

> อ่านไฟล์นี้ก่อนแตะโค้ดฝั่ง Apps Script ทุกครั้ง

## ทำไมต้องแยก 2 โปรเจกต์

Apps Script รวมทุกไฟล์ในโปรเจกต์เดียวกันไว้ใน scope เดียว **ถ้ามีฟังก์ชันชื่อซ้ำกัน
ตัวที่โหลดทีหลังจะทับตัวแรกแบบเงียบ ๆ ไม่มี error** ทั้งสองโปรเจกต์ต่างก็ต้องมี `doPost`
ของตัวเอง จึงอยู่ร่วมโปรเจกต์กันไม่ได้

เคยเกิดขึ้นจริงมาแล้ว: โค้ดบันทึกเหตุการณ์ถูกวางรวมในโปรเจกต์ของหน้าลงยอด `doPost`
ทับกัน ทำให้การลงยอดแมนนวลหยุดเข้าแท็บ `ยอดผลิต` ไปกว่า 1 เดือนโดยไม่มีใครรู้
(รายละเอียดใน PR #41)

## โปรเจกต์ที่ 1 — ลงยอด / Job Monitor

| หัวข้อ | ค่า |
|---|---|
| ชื่อโปรเจกต์ใน Apps Script | `KPI` |
| ประเภท | ผูกกับชีต (container-bound) เปิดผ่านเมนู ส่วนขยาย → Apps Script จากในชีต |
| ไฟล์ต้นฉบับในรีโป | `scr/ลงยอดผ่านแอป.js` (และไฟล์อื่นในโฟลเดอร์ `scr/`) |
| เขียนลงแท็บ | `ยอดผลิต` |
| Web App URL | `https://script.google.com/macros/s/AKfycbyrPeUTHuQu3urqlhbcZnpuNH0b6unvO2nTpVD7vKrGoVSGFMwDYWGnYszZ8dahks7YSg/exec` |
| ใครเรียก | `index.html` (ค่าคงที่ `FIXED_SCRIPT_URL`) |
| รับ action | ไม่ส่ง action = ลงยอด, `close_job`, `undo` |
| ตรวจสุขภาพ | เปิด `URL/exec?mode=health` ในเบราว์เซอร์ |

## โปรเจกต์ที่ 2 — บันทึกเหตุการณ์ / Incident Log

| หัวข้อ | ค่า |
|---|---|
| ชื่อโปรเจกต์ใน Apps Script | ตั้งชื่อให้สื่อ เช่น `Incident Sync` |
| ประเภท | แยกอิสระ (standalone) เปิดจาก script.google.com เท่านั้น ไม่อยู่ในเมนูของชีต |
| ไฟล์ต้นฉบับในรีโป | `gas_sync_incidents.js` |
| เขียนลงแท็บ | `Incidents`, `IncidentCategories` และ `Manpower` |
| Web App URL | `https://script.google.com/macros/s/AKfycbxZqA-HEsacO4Ie0Tn7Mw5tr0Zz1eYpmbza0dgNxyt30Qkku6HoT25ZLChbysUDHoRf/exec` |
| ใครเรียก | `barcode.html` ผ่านค่า `incidentAppsScriptUrl` ใน `config.json` |
| รับ action | `GET_INCIDENTS`, `SAVE`/`DELETE` (ต้องมี `id`), `GET_CATEGORIES`, `SAVE_CATEGORY`, `DELETE_CATEGORY`, `GET_MANPOWER`, `SAVE_MANPOWER`, `DELETE_MANPOWER` |
| ตรวจสุขภาพ | เปิด URL ตรง ๆ ในเบราว์เซอร์ จะได้ JSON บอกว่าผูกกับชีตไหน |

ทั้งสองโปรเจกต์เขียนลง **สเปรดชีตเดียวกัน** คือ `ลงยอด H9`
ID: `1PYcAatoJ4QX28uQ_LF8dDC6oTiMWbfPs5TZDfGJVa4U`

## แท็บ `Manpower`

กำลังคนรายไลน์ แยกตามกะ 1 แถว = 1 วัน + 1 กะ + 1 ไลน์

```
ID = "2026-08-10_Day_Line 5"
คอลัมน์: ID | Date | Shift | Line | Assembler | Feeder | Leader | Total | UpdatedAt
```

ค่าที่เป็นข้อความถูกเขียนโดยนำหน้าด้วย `'` เพื่อไม่ให้ Sheets แปลงชนิดเอง
(บทเรียนจากคอลัมน์ `Date` ใน PR #38 และ `Time` ใน PR #47)

หน้าจอใช้ตัวเลขนี้คำนวณ **PCS/ประกอบ/ชม.** = ยอดสแกน ÷ จำนวนพนักงานประกอบ ÷ ชั่วโมงที่ไลน์นั้นผลิตจริง

ตัวหารนับเฉพาะ `Assembler` ส่วน `Feeder` และ `Leader` ยังต้องกรอกและเก็บไว้ทุกครั้ง
แต่ถือเป็นกำลังสนับสนุน ไม่ใช่คนที่ผลิตชิ้นงานในไลน์ จึงไม่นำมาหาร
คอลัมน์ `Total` ในชีตยังเป็นผลรวมทั้ง 3 บทบาทตามเดิม ใช้ดูกำลังพลจริงหน้างาน

## หาโปรเจกต์ไม่เจอ ทำอย่างไร

1. เปิด https://script.google.com → เมนู **โปรเจกต์ของฉัน (My Projects)** จะเห็นทั้งสองตัว
   โปรเจกต์แบบ standalone จะ**ไม่ปรากฏ**ในเมนู ส่วนขยาย ของชีต จึงต้องเข้าทางนี้เท่านั้น
2. ถ้าจำชื่อไม่ได้ ให้เอา URL จากตารางด้านบนมาเปิดในเบราว์เซอร์
   ตัวบันทึกเหตุการณ์จะตอบ JSON ที่มี `project` และ `spreadsheet` บอกว่าเป็นตัวไหน
3. URL ที่ใช้งานอยู่จริงดูได้จาก `config.json` (บันทึกเหตุการณ์) และ `index.html` (ลงยอด)
   ถ้า URL ในรีโปไม่ตรงกับที่ deploy อยู่ ให้ยึดของที่ deploy แล้วอัปเดตรีโปตาม

## กฎที่ห้ามละเมิด

- **ห้ามรวมโค้ดสองโปรเจกต์เข้าด้วยกัน** ไม่ว่าจะสะดวกแค่ไหน เพราะ `doPost` จะทับกัน
- **ห้ามชี้ URL ของบันทึกเหตุการณ์ไปที่ Web App ของหน้าลงยอด** — `barcode.html` มีตัวกัน
  `isProductionEntryScriptUrl()` ที่จะปฏิเสธและเตือนอยู่แล้ว
- **แก้โค้ดในตัวแก้ไขแล้วต้อง Deploy เวอร์ชันใหม่เสมอ** ไม่งั้น URL เดิมยังเสิร์ฟโค้ดเก่า
- **ห้ามตั้งชื่อฟังก์ชันซ้ำกับที่มีอยู่ในโปรเจกต์เดียวกัน** โดยเฉพาะ `doGet` และ `doPost`

## เครื่องมือล้างข้อมูล

`gas_cleanup_incidents.js` มีฟังก์ชันล้างแถวเสียของทั้งสองแท็บ
วางเป็นไฟล์ใหม่ในโปรเจกต์ไหนก็ได้ (มันเข้าถึงชีตด้วย `openById`)

| แท็บ | ตรวจก่อน | ลบจริง | เกณฑ์แถวเสีย |
|---|---|---|---|
| `Incidents` | `dryRunCleanupIncidents()` | `cleanupIncidents()` | คอลัมน์ `ID` ว่าง |
| `ยอดผลิต` | `dryRunCleanupYod()` | `cleanupYod()` | `Recorder` และ `Job order No.` ว่างทั้งคู่ |

ทั้งคู่สำรองแท็บเป็น `<ชื่อแท็บ>_backup_YYYYMMDD_HHmm` ให้อัตโนมัติก่อนลบ
**รัน dry run ก่อนเสมอ**

## ประวัติปัญหาที่เคยเกิด

| PR | เรื่อง |
|---|---|
| #38 | คอลัมน์ `Date` เป็นชนิดวันที่ ทำให้รายงาน PDF กรองช่วงวันที่ไม่เจอ |
| #39 | POST แบบ `no-cors` อ่านผลไม่ได้ และผลลัพธ์ว่างจากคลาวด์ลบข้อมูลในเครื่อง |
| #41 | `doPost` ทับกัน จนการลงยอดไหลไปลงแท็บ `Incidents` — ที่มาของการแยกโปรเจกต์ |
| #44 | คำขอที่ไม่ใช่การลงยอดกลายเป็นแถว `undefined` ในแท็บ `ยอดผลิต` |
| #45 | ปุ่มลบบันทึกเหตุการณ์ประกอบคีย์ใหม่จนลบไม่ติด |
